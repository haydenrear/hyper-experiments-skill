#!/usr/bin/env python3
"""Project-wide experiment orchestrator with a baselines hook.

This script is the single entry point for running this project's
experiments. It is intentionally split into two phases so the project
can fill them in independently:

  1. ``run_baselines()`` — produce shared baselines that experiments
     compare against. **Default: skip.** Baselines are expensive and
     should be reused, not re-run on every invocation. Only fill this
     in to (re)compute a baseline that does not already exist on disk.
  2. ``run_experiments()`` — run / queue this project's experiments.
     Default: stub. Fill in with whatever orchestration the project
     needs (sequential local runs, queue submission, dispatch to
     RunPod, etc.).

Baselines live under, in priority order of reuse:

  - ``<root>/experiments/baselines/`` — cross-family.
  - ``<root>/experiments/families/<family>/baselines/`` — per-family.
  - per-experiment baselines, produced inside ``code/run_experiment.py``
    via its own ``run_baselines()`` hook.

The ``run_baselines()`` skip default exists by design. Most of the
time the right action is to read the cached baseline from disk and
move on — leaving this function as a no-op is the path of least
resistance, which keeps baseline regeneration deliberate. When you
*do* fill it in, write produced artifacts to the appropriate
``baselines/`` directory and append an entry to its ``index.md`` so
future experiments find them by reference.

Generated by ``init_project.py``. Edit freely; this file is not a
wrapper around the skill.
"""
from __future__ import annotations

import argparse
import sys
from pathlib import Path

ROOT_MARKER = "hyper-experiments.md"


def _project_root() -> Path:
    cwd = Path.cwd().resolve()
    for d in (cwd, *cwd.parents):
        if (d / ROOT_MARKER).exists():
            return d
    raise SystemExit(
        f"error: not inside a hyper-experiments project "
        f"(no {ROOT_MARKER} found walking up from {cwd})."
    )


def run_baselines(root: Path) -> None:
    """Produce shared baselines that experiments will compare against.

    **Default: skip.** Baselines are expensive and should be reused —
    only fill this in when a baseline that an experiment needs does
    not already exist under:

      * ``<root>/experiments/baselines/``                (cross-family)
      * ``<root>/experiments/families/<family>/baselines/``  (per-family)

    When you do fill it in, write the produced artifacts to the
    appropriate baselines directory and update its ``index.md`` so
    future experiments find them by reference rather than by re-running.
    """
    print("run_baselines: skipped — fill in this function to (re)compute "
          "shared baselines. Most of the time the right action is to "
          "reuse what is already cached on disk.")


def run_experiments(root: Path) -> int:
    """Run / queue this project's experiments.

    **Default: stub.** Fill in with project-specific orchestration
    (sequential local runs, queue submission, RunPod dispatch, etc.).
    """
    print("run_experiments: TODO — fill in project-specific run orchestration.")
    return 0


def main() -> int:
    ap = argparse.ArgumentParser(description=__doc__.splitlines()[0])
    ap.add_argument("--skip-baselines", action="store_true",
                    help="Bypass run_baselines() entirely (default still skips, "
                         "this is for when run_baselines has been filled in).")
    args = ap.parse_args()

    root = _project_root()
    if not args.skip_baselines:
        run_baselines(root)
    return run_experiments(root)


if __name__ == "__main__":
    raise SystemExit(main())
