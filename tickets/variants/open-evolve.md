There should be experiment variants with standard and an open-evolve experiment. The open-evolve variant determines which parts of the problem can be evolved.

The starter for those experiments/families will include starter code with instructions. The script will have a --variant which will default to the standard experiment. Branch will just keep the variant of it's parent.

Additionally, there will be a variant flag for the scaffold of the project.
