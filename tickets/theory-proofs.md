Proof should be first class. Mathematical language is first class ~ best data starters.

Hypothesis -> Proof -> Falsifiability Metrics -> Experiment

This is why we collect the metrics. Guardrails to save relevant data -> accumulation of data for comparisons.

CLI should probably have metric library, ability to add/remove. With metric schema. For multi scale loss dataset. So that architecture search is accumulative.

Decorate the tensorboard -> Kafka topic 
-> Downstream dataset producer
-> Downstream dataset consumers
--> Analyzer 
--> Experiment composer

We should add a theories directory with proofs where possible.
